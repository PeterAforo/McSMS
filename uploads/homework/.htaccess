# Allow access to uploaded homework files
Options -Indexes
RewriteEngine Off

# Ensure files are served directly
<FilesMatch "\.(jpg|jpeg|png|gif|webp|pdf|doc|docx|txt)$">
    Require all granted
</FilesMatch>
